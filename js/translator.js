// Translator/replacer legacy module removed.
// CSV/current->replace replacement handling is now performed directly in `js/app.js`.
window.Replacer = {
    // placeholder no-op for backwards compatibility
};